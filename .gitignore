# ── Private keys & generated secrets ─────────────────────────────────────────
clients/
amneziawg/conf/server_private.key
amneziawg/conf/server_public.key
amneziawg/conf/awg0.conf.lock
amneziawg/conf/.next_ip_*
xray/conf/reality_keys.txt
xray/conf/config.json.lock
xray/conf/.conn_info_*

# ── Runtime configs with embedded secrets (use templates for VCS) ────────────
docker-compose.yml
amneziawg/conf/awg0.conf
xray/conf/config.json


# ── VPN operator skill ───────────────────────────────────────────────────────
# Real host inventory holds IPs/keys; canonical copy lives at ~/.config/vpn/.
# Only the template (hosts.example.yml) is committed.
.claude/skills/vpn/hosts.yml
.claude/settings.local.json

# ── Python ───────────────────────────────────────────────────────────────────
__pycache__/

# ── OS / editor ──────────────────────────────────────────────────────────────
.DS_Store
*.swp
*.swo
*~
.idea/
.vscode/
